# HW1 Grid Map 開發與除錯對話紀錄

## 階段一：需求分析與初始實作
*   **使用者提供**：HW1-1 與 HW1-2 的作業說明圖（包含 Grid 產生、設定起始格/終點格/障礙物，以及策略評估 Value Matrix 與 Policy Matrix 的顯示）。
*   **AI 規劃與實作**：
    *   撰寫了 `implementation_plan.md` 設計架構。
    *   假設了基本的 Gridworld 設定：到達終點給予 `+10` 獎勵，平時移動給予 `-1` 懲罰，衰減因子 $\gamma = 0.9$。
    *   建立後端 Flask API (`app.py`) 來處理 `/evaluate` 請求並執行 Policy Evaluation。
    *   建立前端 (`templates/index.html`) 提供使用者互動式網格（點擊設定 Start、End、Obstacles）並以 AJAX 獲取結果畫出兩個矩陣。

## 階段二：環境設定與自動化測試
*   **AI 啟動伺服器**：發現缺少 `flask` 與 `numpy` 套件，執行 `pip install` 安裝依賴環境。
*   **自動化 UI 測試**：AI 使用虛擬瀏覽器打開 `http://127.0.0.1:5000` 進行點擊測試，測試功能是否正常渲染。

## 階段三：根據作業要求進行細節調整
*   **使用者提供進階截圖**：提供參考說明，要求實作「疊代計算」與「視覺化美化（顯示至小數點後兩位）」。
*   **AI 第一次修正**：
    *   把 `app.py` 內的數值疊代更新改成固定跑 100 次的 `for` 迴圈。
    *   在 `index.html` 中將渲染的 Value Matrix 數字套用 `.toFixed(2)`。
*   **使用者詢問邏輯細節**：「有沒有使用迴圈反覆更新？」、「$\gamma$ 設多少？」、「撞牆或出界時是留在原地還是給予額外懲罰？」
*   **AI 解釋與提議**：
    *   回報目前使用 100 次的迴圈。
    *   $\gamma = 0.9$。
    *   撞牆/出界判定為「**留在原地**（next_v = V[r,c]）」並給予正常移動的一步懲罰（`-1`）。
    *   提議將固定 100 次修改為利用變異量 $\Delta < 10^{-3}$ 來提早判斷收斂的更佳做法。
*   **使用者確認 (`好的`)**：同意進行修改。

## 階段四：完成條件收斂與最終驗證
*   **AI 第二次修正**：
    *   在 `app.py` 將迴圈改為檢查前後每次更新的最大差距 `delta`，當 `delta < 1e-3` 時即 `break` 結束計算（並加上 1000 次最大疊代保護）。
    *   將目標地點（Goal）的值強制於畫面上顯示為 `0`。
*   **最終驗證**：
    *   AI 再次使用自動化瀏覽器完成 5x5 地圖的建置（設定起點 1、終點 25、障礙物 12, 14, 16）。
    *   點選「Evaluate Policy」獲取到了正確格式的數值（例如 -9.99）與方向箭頭。
    *   確認 UI 排版與數值均符合要求後，向使用者回報專案已成功修改並運作順利。

## 階段五：HW1-3 最佳政策（Value Iteration）實作與驗證
*   **使用者提供新需求**：提供 HW1-3 截圖，要求實作價值疊代算法以推導最佳政策，並需更新畫面顯示對應的價值函數與行動。
*   **AI 規劃與實作**：
    *   提議在 UI 增加獨立的「Find Optimal Policy」按鈕，讓使用者能比較「隨機政策」與「最佳政策」的差異。
    *   在 `app.py` 中新增了 `/optimal_policy` 路由。
    *   實作完整的 **Value Iteration**：透過 $\max_a$ 更新貝爾曼方程式，直到變異量 $\Delta < 10^{-3}$。
    *   透過 $\text{argmax}_a$ 針對收斂後的價值矩陣萃取出最佳的行動策略（箭頭）。
    *   在 `index.html` 加上新按鈕與對應的 Fetch 邏輯來渲染新的最佳動作矩陣。
*   **最終驗證**：
    *   AI 再次啟動自動化工具建立 5x5 地圖並設定起終點與障礙物。
    *   點選「Find Optimal Policy」後驗證結果，確認目標點附近的值有依照 $\gamma=0.9$ 正確遞增（例如 10.00, 8.00, 6.20 等），且所有的箭頭行動都精確避開障礙物並指向最佳路徑。
    *   向使用者回報實作與測試成功。

--
*本紀錄由 Antigravity 助手於專案資料夾自動彙整產生。*
